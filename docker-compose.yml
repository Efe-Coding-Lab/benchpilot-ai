services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: benchpilot
      POSTGRES_USER: benchpilot
      POSTGRES_PASSWORD: benchpilot
    ports:
      - "5432:5432"
    volumes:
      - benchpilot_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U benchpilot -d benchpilot"]
      interval: 5s
      timeout: 3s
      retries: 10

  app:
    build: .
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgresql+psycopg://benchpilot:benchpilot@db:5432/benchpilot
    ports:
      - "8000:8000"

volumes:
  benchpilot_pg:
